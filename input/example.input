# File names
data_file   = data/test_data.athdf    # file containing Athena++ data dump
output_file = output/test_output.dat  # file to be (over)written with image data

# Coordinates
bh_m  = 1.0     # black hole mass scale for data (usually 1)
bh_a  = 0.9     # black hole spin in units of mass (magnitude less than m)
coord = sph_ks  # simulation coordinates (sph_ks or cart_ks)

# Units
m_msun   = 4.152e6  # black hole mass in solar masses
rho_unit = 1.6e-16  # unit code density in g/cm^3

# Plasma
plasma_mu        = 0.5   # molecular weight of fluid in proton masses
plasma_ne_ni     = 1.0   # electrons per ion
plasma_rat_high  = 10.0  # ion-electron temperature ratio at infinite plasma beta
plasma_rat_low   = 1.0   # ion-electron temperature ratio at zero plasma beta
plasma_sigma_max = 1.0   # if nonnegative, cutoff in sigma above which plasma is considered vacuum

# Image
im_radius = 50.0    # radial coordinate of camera center in gravitational units
im_theta  = 135.0   # polar coordinate of camera center in degrees (0/180 for north/south)
im_phi    = 0.0     # azimuthal coordinate of camera in degrees
im_rot    = 0.0     # rotation of camera in degrees (0/90 for north up/left)
im_width  = 24.0    # full width of image in gravitational units
im_res    = 64      # number of pixels per side
im_freq   = 2.3e11  # frequency of observation in GHz

# Rays
ray_step          = 0.01   # affine step size along each geodesic in units of current radius
ray_max_steps     = 1500   # maximum number of steps allowed for each geodesic
ray_sample_interp = false  # flag indicating resampling should use intrablock interpolation
ray_flat          = false  # flag indicating ray tracing should assume flat spacetime

# Performance
num_threads = 4  # number of threads to use in parallel
